# خطة لوحة التحكم (Admin Dashboard) - Frontend

## 🎯 الهدف
إنشاء لوحة تحكم ويب للتعامل مع الـ Backend الموجود حالياً لإدارة:
- المستخدمين
- شركات التأمين
- طلبات الأدوية

---

## 🛠️ التقنيات المستخدمة

| التقنية | السبب |
|---------|-------|
| **React** (Vite) | سهولة التطوير وسرعة الأداء |
| **JavaScript** | بدون TypeScript للسرعة |
| **Tailwind CSS** | تصميم سريع ومرن |
| **React Query** | إدارة الـ API calls والـ caching |
| **React Router** | التنقل بين الصفحات |
| **Axios** | التعامل مع الـ API |
| **React Hook Form** | إدارة الـ Forms |
| **Recharts** | عرض الإحصائيات |

---

## 📐 هيكل المشروع

```
admin-dashboard/
├── public/
├── src/
│   ├── api/                    # API Service Layer
│   │   ├── axiosInstance.js
│   │   ├── users.api.js
│   │   ├── insurance.api.js
│   │   └── orders.api.js
│   │
│   ├── components/             # Reusable Components
│   │   ├── layout/
│   │   │   ├── Sidebar.jsx
│   │   │   ├── Header.jsx
│   │   │   └── DashboardLayout.jsx
│   │   ├── common/
│   │   │   ├── Button.jsx
│   │   │   ├── Modal.jsx
│   │   │   ├── Table.jsx
│   │   │   ├── Pagination.jsx
│   │   │   ├── SearchBar.jsx
│   │   │   └── StatusBadge.jsx
│   │   └── charts/
│   │       ├── StatsCard.jsx
│   │       └── OrdersChart.jsx
│   │
│   ├── pages/                  # Page Components
│   │   ├── DashboardPage.jsx
│   │   ├── users/
│   │   │   ├── UsersListPage.jsx
│   │   │   └── UserDetailsPage.jsx
│   │   ├── insurance/
│   │   │   ├── InsuranceListPage.jsx
│   │   │   └── InsuranceFormPage.jsx
│   │   └── orders/
│   │       ├── OrdersListPage.jsx
│   │       └── OrderDetailsPage.jsx
│   │
│   ├── hooks/                  # Custom Hooks
│   │   ├── useUsers.js
│   │   ├── useInsurance.js
│   │   └── useOrders.js
│   │
│   ├── utils/                  # Helper Functions
│   │   ├── formatDate.js
│   │   └── constants.js
│   │
│   ├── App.jsx
│   └── main.jsx
│
├── package.json
└── tailwind.config.js
```

---

## 📄 الصفحات المطلوبة (7 صفحات)

### 1. 📊 الصفحة الرئيسية (Dashboard)
```
المميزات:
- بطاقات إحصائية (Stats Cards):
  • إجمالي المستخدمين
  • إجمالي شركات التأمين
  • إجمالي الطلبات
  • الطلبات المعلقة (Pending)
  
- رسم بياني للطلبات حسب الحالة
- آخر 5 طلبات
- آخر 5 مستخدمين مسجلين

الـ Endpoints المستخدمة:
- GET /api/users (count)
- GET /api/insurance (count)
- GET /api/orders (client-side stats)
```

---

### 2. 👥 صفحة المستخدمين (Users)

#### قائمة المستخدمين
```
المميزات:
- جدول يعرض: الاسم، الإيميل، الجنس، تاريخ الميلاد، النقاط
- بحث بالاسم أو الإيميل
- Pagination
- زر عرض التفاصيل
- زر حذف

الـ Endpoint: GET /api/users
```

#### تفاصيل المستخدم
```
المميزات:
- عرض كل بيانات المستخدم
- عرض بطاقات التأمين المرتبطة به
- عرض طلباته
- تعديل النقاط

الـ Endpoints:
- GET /api/users/:id
- PUT /api/users/:id
- DELETE /api/users/:id
```

---

### 3. 🏥 صفحة شركات التأمين (Insurance)

#### قائمة الشركات
```
المميزات:
- جدول يعرض: اللوجو، الاسم، الوصف، الحالة (Active/Inactive)
- زر إضافة شركة جديدة
- زر تعديل
- زر حذف
- Toggle للتفعيل/التعطيل

الـ Endpoint: GET /api/insurance
```

#### فورم إضافة/تعديل
```
المميزات:
- حقل الاسم
- حقل الوصف
- رفع صورة اللوجو
- Checkbox للتفعيل

الـ Endpoints:
- POST /api/insurance (إضافة)
- PUT /api/insurance/:id (تعديل)
- DELETE /api/insurance/:id (حذف)
```

---

### 4. 💊 صفحة الطلبات (Orders)

#### قائمة الطلبات
```
المميزات:
- جدول يعرض: رقم الطلب، اسم المستخدم، اسم الدواء، الكمية، الحالة، التاريخ
- فلترة حسب الحالة (All, Pending, Approved, Rejected)
- بحث
- Pagination
- Status Badge ملون حسب الحالة
```

#### تفاصيل الطلب
```
المميزات:
- عرض كل تفاصيل الطلب
- عرض صورة الروشتة إن وجدت
- أزرار تغيير الحالة (Approve / Reject)
```

---

## 🎨 تصميم الـ UI

### Sidebar (القائمة الجانبية)
```
┌─────────────────────┐
│      LOGO           │
├─────────────────────┤
│ 📊 Dashboard        │
│ 👥 Users            │
│ 🏥 Insurance        │
│ 💊 Orders           │
└─────────────────────┘
```

### Color Scheme
```
Primary:    #10B981 (Green - Healthcare theme)
Secondary:  #3B82F6 (Blue)
Success:    #22C55E
Warning:    #F59E0B
Danger:     #EF4444
Background: #F3F4F6
Dark:       #1F2937
```

---

## 📋 خطة التنفيذ

### المرحلة 1: الإعداد الأساسي (يوم 1)
- [ ] إنشاء المشروع بـ Vite + React
- [ ] تثبيت المكتبات (Tailwind, React Router, Axios, etc.)
- [ ] إعداد هيكل المجلدات
- [ ] إنشاء الـ Axios Instance مع الـ Base URL

### المرحلة 2: الـ Layout (يوم 2)
- [ ] إنشاء الـ DashboardLayout (Sidebar + Header)
- [ ] إعداد الـ Routes
- [ ] تصميم الـ Sidebar
- [ ] تصميم الـ Header

### المرحلة 3: الصفحة الرئيسية (يوم 3)
- [ ] إنشاء Stats Cards
- [ ] إنشاء جدول آخر المستخدمين
- [ ] إنشاء جدول آخر الطلبات
- [ ] ربط البيانات من الـ API

### المرحلة 4: صفحات المستخدمين (يوم 4)
- [ ] إنشاء جدول المستخدمين مع Pagination
- [ ] إضافة البحث
- [ ] صفحة تفاصيل المستخدم
- [ ] وظيفة الحذف

### المرحلة 5: صفحات شركات التأمين (يوم 5)
- [ ] إنشاء جدول الشركات
- [ ] فورم الإضافة/التعديل مع رفع الصور
- [ ] وظيفة الحذف
- [ ] Toggle التفعيل

### المرحلة 6: صفحات الطلبات (يوم 6)
- [ ] إنشاء جدول الطلبات
- [ ] الفلترة حسب الحالة
- [ ] صفحة تفاصيل الطلب
- [ ] عرض صورة الروشتة

---

## 🚀 البداية السريعة

```bash
# إنشاء المشروع
npm create vite@latest admin-dashboard -- --template react

# الدخول للمجلد
cd admin-dashboard

# تثبيت المكتبات الأساسية
npm install axios react-router-dom @tanstack/react-query

# تثبيت Tailwind
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# تشغيل المشروع
npm run dev
```

---

## 📊 ملخص

| العنصر | العدد |
|--------|-------|
| **الصفحات** | 7 صفحات |
| **الـ Components** | ~12 component |
| **الـ API Calls** | ~10 endpoints |
| **الوقت المقدر** | 5-6 أيام عمل |
